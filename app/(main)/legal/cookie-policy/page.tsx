import CookiePolicy from "@/components/Legal/CookiePolicy";

export const metadata = {
  title: "Cookie Policy | Qatobit",
  description: "How Qatobit uses cookies and similar tracking technologies on its platform.",
};

export default function CookiePolicyPage() {
  return <CookiePolicy />;
}
